# ── Web server ─────────────────────────────────────────────────────
flask
gunicorn

# ── Dashboard & Visualization ──────────────────────────────────────
plotly
pandas

# ── DistilBERT inference (CPU-only) ───────────────────────────────
# Install CPU-only PyTorch (~300MB instead of ~2.5GB CUDA build)
--extra-index-url https://download.pytorch.org/whl/cpu
torch --index-url https://download.pytorch.org/whl/cpu
transformers
safetensors
tokenizers

# ── Config ─────────────────────────────────────────────────────────
pyyaml

# ── Database (PostgreSQL) ──────────────────────────────────────────
psycopg2-binary
sqlalchemy

# ── SVM dependencies (only installed if SVM model is detected) ────
# These are NOT listed here — they are conditionally installed
# in the Dockerfile only when the model is sklearn/SVM:
#   scikit-learn==1.4.2 joblib==1.4.2 numpy==1.26.4
#   sentence-transformers==3.0.1