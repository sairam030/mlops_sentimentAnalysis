# Serving-only dependencies (slim — no training libs)
fastapi==0.115.*
uvicorn[standard]==0.34.*
pydantic>=2.0
httpx>=0.27  # Required by FastAPI TestClient

# Model loading
mlflow>=2.0
boto3>=1.35

# DistilBERT inference
transformers>=4.40
torch>=2.0
tokenizers

# SVM inference (only needed if SVM is best model)
scikit-learn>=1.5
joblib
sentence-transformers>=3.0
vaderSentiment

# Config
python-dotenv
pyyaml
numpy
